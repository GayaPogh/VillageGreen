<header class="navbar  py-2  shadow-sm ">
	<div class="container d-flex justify-content-center align-items-center">
		<a href="{{ path('app_accueil') }}" class="logo-link d-flex align-items-center">
			<img src="{{ asset('images/Logo.png') }}" alt="VillageGreen Logo" style="height: 80px;">
			<span class="logo-text ms-2">
				<span class="village">Village</span>
				<span class="green">Green</span>
			</span>
		</a>
	</div>
	<!-- Navbar -->
	<div class="navbar-bottom col-md-12">
		<nav class="navbar navbar-expand-lg">
			<div class="container-fluid">
				<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo03" aria-controls="navbarTogglerDemo03" aria-expanded="false" aria-label="Toggle navigation">
					<span class="navbar-toggler-icon"></span>
				</button>
				<div class="collapse navbar-collapse" id="navbarTogglerDemo03">
					<div class="d-flex w-100 justify-content-between align-items-center">
						<ul class="navbar-nav mb-2 mb-lg-0">
							<li class="nav-item">
								<a class="nav-link active" href="{{ path('app_accueil') }}">Accueil</a>
							</li>
							<li class="nav-item">
								<a class="nav-link" href="{{ path('categorie_show', {id: 1}) }}">Catégories</a>
							</li>
							<li class="nav-item">
								<a class="nav-link" href="#">À propos</a>
							</li>
							<li class="nav-item">
								<a class="nav-link" href="#">Contact</a>
							</li>
						</ul>
					</div>
				</div>

				<!-- 🔎 Search form -->
				<div class="search-container ms-3">
					<form action="{{ path('search_products') }}" method="get" class="d-flex">
						<input type="text" name="q" class="form-control me-2" placeholder="Rechercher..." value="{{ app.request.get('q') }}">
						<button class="btn btn-outline-success" type="submit">🔍</button>
					</form>
				</div>


				<!-- Icons -->
				<div class="icons ms-3 d-flex align-items-center">
					<a href="#" class="ms-2">
						<img width="20" height="20" src="https://img.icons8.com/forma-bold/24/user.png" alt="user"/>
					</a>
					<a href="#" class="ms-2">
						<img width="20" height="20" src="https://img.icons8.com/forma-bold/24/hearts.png" alt="hearts"/>
					</a>
					<a href="{{ path('panier_index') }}" class="position-relative d-inline-block ms-2">
						<img width="20" height="20" src="https://img.icons8.com/forma-bold/24/shopping-cart.png" alt="shopping-cart"/>
						{% if panier_count() > 0 %}
							<span class="badge bg-danger">{{ panier_count() }}</span>
						{% endif %}


					</a>


				</div>
			</div>
		</nav>
	</div>
</header>
